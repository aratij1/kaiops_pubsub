global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/alert-rules.yml

scrape_configs:
  - job_name: kaiops-fault-lab
    static_configs:
      - targets: ["fault-lab:8080"]
